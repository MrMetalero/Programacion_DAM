import java.sql.SQLException;

public abstract class AddUser {

    public static boolean adduser(Usuario usuario) throws SQLException{
    
        String sentenciaSqlBase = "INSERT INTO usuarios (id, column2, column3, ...)"
            +"VALUES (value1, value2, value3, ...);";
        String sentenciaSql
        if (sentenciaSQLbase != "") {
            sentenciaSQL = "INSERT INTO usuarios (id, column2, column3, ...)"
            +"VALUES (value1, value2, value3, ...);"
            
            
            
            ;

            
        }else{
            System.out.println("Fallo, no existe sentencia sql");
        }

        /*
         * Sales department
         * Victor is Manuel's assistant
         * He's in a meeting all afternoon
         * until at around 6:30 pm
         * check a couple things with him
         * 07318972
         * 
         * 
         */

        return true;
    }



    
}